# fly.toml - NYSS Bakes WhatsApp Bot
app = "nyss-bakes-whatsapp"
primary_region = "jnb"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "5000"
  PYTHONUNBUFFERED = "1"

[http_service]
  internal_port = 5000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"

[vm]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1